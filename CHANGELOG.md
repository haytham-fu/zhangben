# 更新日志

## 2026-09-17（关联设备 + 流水按日分组 + UI 修复）

- 设置「关联设备」：本机名称与稳定设备 ID；生成关联码并下载 `zhangben-sync-<码>.json`；复制 `?pair=` 配对链接；另一台输入关联码或导入文件合并；「立即同步」支持链接拉取或选文件；可选同步 URL + 打开时自动拉取；展示已知设备列表
- 说明：关联后靠同一同步文件/链接对齐，不是账号云实时同步
- 个人计划包：去掉重复的 1000 HKD 八达通充值，仅保留 857.3 RMB 充值（见 zhangben-profile）
- 流水明细：按日期分组展示（日期标题 + 当日支出小计），新→旧排序
- 流水筛选：新增「预算分区」——基础生活支出 / 专项支出；有小荷包时按荷包逐个筛选
- 动效：按钮 / 芯片 / 底栏点按缩放更轻、过渡更长更柔（液态玻璃手感保留）
- 修复：记录详情 / 编辑 / 筛选弹层底部「退出」等操作区抬高 z-index、不透明页脚与 isolation，避免被底栏 / sticky / 滚动层穿模裁切

## 2026-09-17（个人计划包可合并流水）

- 计划配置：`LedgerProfilePack.transactions?` 可选；导入按 id 合并流水（跳过重复），设置成功提示含新增笔数
- 公网空白默认不变；个人明细仍走独立 profile 仓库，不随 Pages 分发

## 2026-09-17（金额输入清空 + 本地数据迁移）

- 修复：预算/日计划/固定汇率数字框为 `0` 时无法用退格清空（本地草稿字符串，失焦空值存 0，聚焦全选）
- 修复：`zhangben-v2` 为空时自动从 `zhangben-v1` 迁移，避免升 key 后重新打开像被清零；不再再次 bump 存储键


## 2026-09-17（液态玻璃控件软化 + 流水编辑）

- UI：弱化芯片/按钮/筛选条的硬边与实心底，改为更贴合液态玻璃的半透明填充与轻阴影，减少「贴纸感」
- 动效：页签切换改为更短、更柔的淡入上移（去掉卡片弹跳缩放），玻璃卡片入场更自然
- 流水明细：记录详情支持「编辑」；可改金额、分类、支付、日期、备注、预算桶与特例/月度等；保存后总览/日历/顶栏剩余即时联动


## 2026-09-17（顶栏剩余 + 空白默认 + 底栏点按）

- 顶栏副标题：预算已设时显示**实时剩余**（`monthStats.totalRemain`），不再只显示静态「3500 + 1500」封顶；未设预算显示「预算未设置」
- 空白账本默认：基础/专项预算与日计划均为 0，计划对照关闭，`monthOpening` 为空；`STORAGE_KEY` 升至 `zhangben-v2`（一次性丢弃旧 localStorage）
- 底栏：仅按钮 `onClick` 切页（去掉 pointer-drag / setPointerCapture）；液态 blob 随 `active` 弹簧滑动；blob `pointer-events: none`，按钮 `z-index` 可点
- 日历 / 设置 / 导出：计划对照统一为 `dailyPlanCompareEnabled === true`（与总览一致）
- 总览「本月已用 / 本月剩余」与进度条一律走 `monthStats`，记账后即时更新

## 2026-09-17（动效与修复）

- 修复：底栏点按切页（延迟 pointer capture，液态玻璃随 transform 弹簧滑向目标项）
- 修复：日历/日计划用本地正午解析日期，避免 UTC 午夜导致周几错位与「剩余」不准
- 加固：个人计划导入校验（normalizeSettings）、标签消毒、仅 https、超时与体积限制
- 底栏液态玻璃：手指滑动跟随 + 行进方向拉伸变形，松手弹簧吸附最近页签（尊重减少动态）
- 全站动效更 Q 弹（页面/卡片/按钮/弹层 spring 过冲）；页面向左右滑可切页签

## 2026-09-17

- 修复：记账后总览/日历/进度条/剩余额度实时联动（页签保活 + 派生 `monthStats` + 日期规范化）
- 公网改为通用账本默认（无个人 monthOpening / 无强制 3500+1500 日计划）；设置支持「导入个人计划配置」（文件 / 链接 / 粘贴）
- 个人计划包改独立仓库托管，不随 Pages 分发


## 2026-09-16

- 删除九月前半月样例流水；公网页从今天起记账
- 期初：`settings.monthOpening` 写入 9/1–9/15 基础/专项已用汇总（无逐日明细）
- 部署：配置 GitHub Pages（`base`/`manifest` 支持 `/zhangben/`），Actions 自动发布
- 弹层：底部操作区改为固定页脚（flex），去掉 scroll 容器内 sticky，滚动时按钮不再跟随抖动
- 滚动流畅：清理永久 will-change、触摸 manipulation、主滚动/overscroll；弹层打开时暂停动态背景动画（液态玻璃模糊强度不变）

- 总览「本月预算」：去掉小金山金砖插画，改为与「今日」同款进度条 + 已用/剩余展示；设置开关改名「显示本月预算进度」
- 小金山：软香槟金砖重设计——圆角矩形、轻高光、液态玻璃底，去掉土豪渐变与夸张弹跳；砖数仍随剩余预算减少
- 小金山：改为整齐堆叠金砖墙（矩形金砖+斜面高光），铺满「本月预算」卡片顶部；剩余预算下降时逐层减少砖块
- 买菜调料：填写容量/用量（ml/L/g/kg/勺/瓶），按保守公式实时估算「大约可吃 X 顿」；可手动改顿数
- 日用品记账：点选牙膏/沐浴露/洗发水/洗衣液等，容量估算「约可用 X 天/次」并写入备注
- 文案：去掉awkward「芯片」提示，改为「点选上方品类添加」
- 小荷包页：卡片间距加大，避免介绍卡与基础/专项贴在一起
- 记账 hub：支出/收入主按钮加大加高，更易点按

- 小荷包：存钱罐模式——创建仅名称+颜色；转入/转出本月预算剩余；系统「小钱猪」月末结算盈余/超支（余额可为负表示欠小钱猪）
- 买菜/做饭均摊：支出→吃饭→在外吃饭或自己做饭；自己做饭可购置食材入库、点选库存记一顿并扣减；小荷包页「冰箱/食材」；弹层均有返回/退出
- 小金山：改为软金色金块/金锭堆叠造型；剩余预算驱动堆高；金额变化有弹跳堆叠动画（尊重减少动态）
- 动态背景：色块更大、移动更快更明显，并增加第三层柔和金色光斑（尊重减少动态）
- 记账顶栏：偏好外币各有一个「币种 → 人民币」换算盒（金额输入 + 实时折合）
- 小荷包：去掉空列表「还没有小荷包」提示文案（保留 + 新建）
- 弹层：居中全显（z-index 10000）；柔和淡入背景 + spring 缩放滑入
- 流水明细：筛选改为折叠面板（收支 / 支付方式），按钮显示摘要与角标
- 顶栏副标题与预算标签随 basicBudget / specialBudget 动态更新
- 记账顶栏预算/汇率简化：中文标签、紧凑布局、固定汇率藏在「调整」；偏好币种显示港币/美元等
- 底栏液态玻璃切换更 Q 弹（spring），尊重减少动态效果
- 小金山更卡通可爱（圆润金堆、表情与闪光），仍随剩余预算缩放
- 去掉设置「音乐会员」自动入账与「关于」；记账时可标「月度支出」计入专项
- 记账 hub「支出 / 收入」同款主按钮样式
- 动态背景流动更明显（仍尊重 prefers-reduced-motion）
- 记账页顶栏：预算概览 + 汇率模式 + 偏好币种（最多 4 个）
- 多币种：RMB / HKD / USD / EUR / JPY / GBP / TWD / MOP / SGD；实时/固定汇率 + 回退
- 全站液态玻璃 UI；底栏切换有滑动玻璃胶囊（尊重减少动态效果）
- 流水明细：无「充值」筛选；底栏文案「流水明细」
- 总览：节奏建议置顶 + 流动背景；小金山 + 设置开关
- 外观：静态/动态背景、多配色主题
- 快捷指令深链入账；文档 `docs/苹果快捷指令.md`
- 小荷包：与基础/专项平级
- 向导无步骤文案；分类/支付弹层盖住底栏；无总览 FAB
- 设置：计划对照、JPG/JSON 备份；预算汇率入口迁至记账顶栏
