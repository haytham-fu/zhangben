# 账本

个人记账对比 Progressive Web App（PWA）。移动优先、简体中文、本地持久化，无需登录。

月预算默认 **5000 RMB** = **3500 基础生活** + **1500 专项**。

## 本地运行

```bash
cd zhangben
npm install
npm run dev
```

浏览器打开终端提示的本地地址（一般为 `http://localhost:5173`）。

生产构建：

```bash
npm run build
npm run preview
```

## 添加到主屏幕（安装 PWA）

构建并部署到 HTTPS（或本机 `preview`）后，可用浏览器安装。开发模式下部分浏览器对 Service Worker 支持有限，建议用 `build` + `preview` 或部署后体验安装。

### iPhone / iPad（Safari）

1. 用 **Safari** 打开网站（需 HTTPS 或 localhost）。
2. 点底部分享按钮（方框带向上箭头）。
3. 下滑选择 **「添加到主屏幕」**。
4. 名称确认为「账本」后点 **添加**。
5. 从主屏幕图标打开，即可全屏独立使用。

### Android（Chrome）

1. 用 **Chrome** 打开网站。
2. 点右上角 **⋮** 菜单。
3. 选择 **「安装应用」** 或 **「添加到主屏幕」**。
4. 确认安装后，可从桌面/应用抽屉打开。

主题色：`#3b82f6`（蓝色）。图标与 Web App Manifest 已随构建生成。

## 预算规则摘要

| 项目 | 说明 |
|------|------|
| 基础 3500 | 吃饭、咖啡/奶茶、交通、洗衣、空调、买菜等 |
| 专项 1500 | 会员订阅、日用品、家具电器、爱好（模型）等 |
| 日计划 | 一/三≈131，二≈96，四/五≈76，六玩≈280 / 不玩≈11.4，日≈56.4（可在设置改） |
| 周六模式 | 可在总览页切换「出去玩 / 不玩」，影响当日计划 |
| 音乐会员 | 默认月初一次性计入专项（48 HKD），可关/可改金额 |
| 空调 | 仅手动记账，不自动按天生成 |
| 特例（请客） | 计入总额；建议可切换「含特例 / 不含特例」 |

## 汇率

- 默认币种 RMB；支持 HKD、USD。
- 默认 **1 HKD = 0.86 RMB**（设置里可改）。
- 每笔保存 **原币金额 + 当时汇率 + 折合 RMB**。

## 截图 OCR 入账（主流程）

1. 打开「记账」→ 默认进入 **截图识别入账**。
2. 选择来源：八达通（默认 HKD）或一般账单；可一次上传多张图片。
3. 等待本地 OCR（Tesseract.js，中英；首次需联网下载语言包）。
4. 在复核列表中勾选、改日期/金额/分类/支付方式；充值默认标为 **不计支出**。
5. 点 **确认导入** 后才写入账本。手动录入与文本批量作为备用。

支付方式可选：八达通、微信支付、支付宝、银行卡、信用卡、其他。

## 八达通规则

- 消费按 **HKD** 录入，按汇率折合 RMB，可标支付方式（八达通 / 微信 / 支付宝 / 银行卡 / 信用卡 / 其他）。
- **充值不计支出**：选「八达通充值」，或批量粘贴时备注含「充值」——记入流水但不计入预算支出。
- 支持批量粘贴：每行 `金额 备注` 或 `日期 金额 备注`；备注含洗衣/空调/吃饭等会尝试自动分类。

## 界面

- 毛玻璃卡片 + 蓝色渐变背景。
- 按钮与芯片均为大圆角 / 胶囊形。
- 日计划与月度 3500 / 1500 / 5000 使用进度条；颜色：安全偏蓝绿 → 临近橙 → 超支橙红 → 严重超支红。

## 数据

全部保存在浏览器 **localStorage**。可在「设置」导出 / 导入 JSON 备份，或清空本地数据。

## 技术

- React + TypeScript + Vite
- `vite-plugin-pwa`（manifest + Service Worker）
